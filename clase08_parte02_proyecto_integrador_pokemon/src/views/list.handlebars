<h2>List Pokemons</h2>

<div>
    {{#each pokemons}}
    <div>
        <a href="/pokemon/{{this._id}}">
            <b>{{this.name}}</b>
            <img src="{{this.photo}}" height="50">
            <i>{{this.type}}</i>
        </a>
        <a href="/pokemon/delete/{{this._id}}">🗑</a>
    </div>
    {{/each}}
</div>